---
description: Deploys the current changes live to GitHub Pages.
---

1. Commit your changes.
2. Push to the `main` branch.

```bash
git add .
git commit -m "update website"
git push origin main
```

The GitHub Action will then automatically build and deploy the site.
